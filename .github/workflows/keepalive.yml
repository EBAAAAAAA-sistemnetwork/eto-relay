name: Keep relay alive
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o /dev/null -w '%{http_code}' https://glorious-space-barnacle-4qjxqj7xq75jf7xvw-3000.app.github.dev/gun || true
